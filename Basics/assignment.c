#include <stdio.h>

int main() {
   float celsius,f;
    scanf("%f", &celsius);
    f=((celsius*9)/5)+32;

    printf("%.1f Celsius = %.1f Farenheit",celsius,f);// Calculate fahrenheit


    // Calculate fahrenheit

    // Print the results
    return 0;
}